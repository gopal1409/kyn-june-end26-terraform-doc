##lets create the vnet. 
a3.vnet.tf
#####################
resource "azurerm_virtual_network" "vnet" {
  name                = "gopal-network"
  location            = azurerm_resource_group.myrg.location
  resource_group_name = azurerm_resource_group.myrg.name
  address_space       = ["10.0.0.0/16"]
 #geeting the refrence from tfstate file
}

#after that do 
terraform apply 
###this will create the vnet. 
##next we need to create the subnet 
##subnet required two argument need to pass as per the terraform documentation
https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs/resources/subnet
##create a new file a4.subnet.tf
##copy the subnet block from line 17 documenation. 
###change the subnet refrence block to web_subnet 
##map the vnet and resourcegroup
terraform apply
